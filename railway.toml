[build]
  builder = "NIXPACKS"

[build.env]
  PYTHON_VERSION = "3.11"

[deploy]
  healthcheckPath = "/health"
  healthcheckTimeout = 300
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
