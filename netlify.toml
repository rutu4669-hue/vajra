[build]
  command = "cd frontend && npm install --legacy-peer-deps && npm run build"
  publish = "frontend/out"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "https://vajra-backend.onrender.com/api/:splat"
  status = 200
  force = true
